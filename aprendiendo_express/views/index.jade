html
	head
		title Mi primer página con Express
	body
		form(action="/form", method="POST")
			input(type="submit", value="Enviar")








		//-Todo lo que está de aqui en adelante son ejemplo de como imprimir arreglos
		//-var arreglo = [1,2,3,4,5];
		//-for (var i = arreglo.length - 1; i >= 0; i--) 
			//p!= "<h1>"+arreglo[i]+"</h1>"
			//p Hola #{arreglo[i]}
		//-if (true)
			p.
				Hola haz click en #[a(href="google.com") este link ] por favor
			each item in arreglo
			p= item
			case item
				when 1
					p El item era uno
				when 2
					p El item era dos
				default
					p El item no era ni uno ni dos 
			